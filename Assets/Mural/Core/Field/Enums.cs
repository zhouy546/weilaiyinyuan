using System.Collections;
using System.Collections.Generic;
using UnityEngine;


namespace nobnak.Blending.Field {

    public enum KwOutput { None = 0, OUTPUT_CORNER_UV, OUTPUT_BLEND_COLOR }
    public enum KwWireframe { None = 0, WIREFRAME_GRID, WIREFRAME_CORNER }
    public enum KwTextureBlend { None = 0, TEXTURE_WITH_MASK, TEXTURE_MASK_ONLY }
}
